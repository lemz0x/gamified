node_modules
dist
dist-ssr
*.local

# env / secrets — keep NOTION_TOKEN and friends out of the repo
.env
.env.*
!.env.example

# persisted server state (producer rundown + roster)
.data/

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code — keep project-level launch.json etc. in the repo, but
# settings.local.json is the user's per-machine permission whitelist.
.claude/settings.local.json

# build caches
.vite
*.tsbuildinfo
.worktrees/

# Stray images — not referenced in source
Gamified_logo.png
Intro.png

# Hermes agent artifacts — not part of the Gamified app
.hermes/
learning-loop/
adversarial-review-pipeline-upgrade-*.md
feedback-learnings-backup-*.md
gamified-daily-sweep-recovery-report.md
gaming-news-last7days.md
pipeline-upgrade-plan-*.md

# Large historical assets — mockups, raw sound drafts, screenshots
# Active SFX files are in public/sfx/. Active fonts are in public/fonts/.
mockups/
assets/

# Untracked planning artifacts (large images, base64 dumps, HTML mockups)
# Tracked planning docs in _planning/ are NOT excluded
_planning/*_b64.txt
_planning/full-overlay.png
_planning/guest-*.png
_planning/*.html
